# 更新日志

## 1.1.0

- 恢复 Apple Music 歌词匹配：搜索结果 ID 会继续传递到歌词接口，支持批量匹配和歌词候选预览，优先使用逐字歌词。
- Apple 歌词解析参考 Lyrico-Plugins 调整为优先处理 PaxSenix `ttmlContent`，支持 Apple `<p>/<span>` 词级时间轴，并回退到多人增强 LRC、增强 LRC 和普通 LRC。
- Apple 歌词候选与 QQ、酷狗和网易云进入同一批并行查询，统一按匹配质量排序，不再专门把 Apple 延后或设为最低来源；增加显式开发者令牌配置，网页脚本提取作为兜底。
- 限制在线文本、加密歌词和封面响应大小，封面缓存限制为 32 MB；批量匹配改为最多 12 个线程且只保持有限数量任务在途，日志限制为 20 万字符。
- 标签保存改为同目录临时文件写入、回读校验后再替换原文件；M4A 的 `newPosition > limit` 失败会保留原文件并显示可读提示。
- 修复 M4A 空曲目号/碟号写入导致的 `Value is not a number as required`，空数字字段现在删除对应 atom；已用合成 AAC/M4A 和 3,750,869 字节边界样本验证写入回读。

## 1.0.1

- 修复部分 SPW/Windows 环境中工作台和文件夹管理窗口的全部文字不可见。
- 随插件内置未修改的 MiSans Medium，用户无需在系统中安装字体；发行包同时保留小米原始字体许可协议。
- 同时提供不含字体、自动使用系统中文字体的轻量安装包；两个包的功能与版本完全相同。
- 包内 MiSans 只有通过 Java2D 中文及拉丁字符实际栅格绘制检测后才会启用；加载或绘制异常时依次回退到系统 MiSans、Microsoft YaHei UI、Microsoft YaHei、Noto Sans CJK SC 和 Java Dialog。
- 为标签、按钮、输入框、菜单、列表、表格、标题边框及日志等 Swing 组件显式设置字体和深浅色前景色，不再继承宿主环境中可能异常的字体或透明文字颜色。
- 插件窗口关闭后仍会恢复此前的 Swing 字体及颜色默认值，避免影响 SPW 本体。

## 1.0.0

这是自 GitHub Release 0.4.0 以来的首个正式整合版本。

- 接入 QQ 音乐、酷狗音乐、网易云音乐和 Apple Music；新增聚合源，按来源补齐标题、艺术家、专辑、扩展标签、封面与歌词。
- 重做歌曲识别流程：优先检索完整标题，再检索括号或影视说明前的核心标题；标题不足时才追加艺术家检索，并结合标题相似度、艺术家、专辑和音频时长选择结果。
- QQ QRC、酷狗 KRC、网易云 YRC 支持真实逐字歌词，逐字不可用时自动回退普通逐行歌词；保留原文、译文与可用的罗马音轨道。
- 新增歌词高级预览：多源结果表、双击载入、逐字/逐行 LRC、ESLyric、SRT、ASS、纯文本、时间偏移、原文/译文/罗马音筛选及繁简转换。
- 新增多源封面预览，展示各平台实际返回图片、像素和文件大小；支持选择应用、导入本地封面及导出当前封面。
- 新增基于 SPW 已加载 BASS 引擎的左右声道频谱图与时间轴总览，不随插件分发额外音频解码器。
- 新增批量勾选、表头全选、按区域右键匹配、标签模糊筛选、失败歌曲置顶、重新扫描列表及展开式详细歌曲列表。
- 批量任务显示实时进度；成功项自动取消勾选，失败或不完整项保留勾选，便于继续处理。
- 新增内存临时日志，按成功、警告、失败和异常记录歌曲、来源、处理范围、失败原因、数量、线程及耗时；关闭工作台后清空。
- 支持多音乐文件夹管理、启动自动扫描、手动添加音频、重载列表及跟随 SPW 当前播放文件。
- 支持标题、艺术家、专辑、专辑艺术家、年份、音轨号、碟号、风格、作曲家、作词家、注释、歌词和封面的预览、编辑与按当前/勾选歌曲写入。
- 配置页新增“跳过已有完整标签歌曲”，标题、艺术家、专辑、封面和歌词均存在时可跳过在线匹配。
- 深浅色、MiSans、圆角组件、圆角弹出菜单、沉浸滚动条和页面动画统一适配；动画按当前显示器刷新率调度。
- 文件扫描、标签读取、在线匹配、封面与歌词候选及频谱计算均异步执行，并按 CPU 逻辑线程动态并发，最高 32 个工作线程。
- 完善页面切换异常恢复与生命周期管理；关闭预览页或工作台时取消任务并释放计时器、监听器、图片、频谱、缓存和窗口引用。
- 主许可证确认为 GPL-3.0-only，补充参考项目和运行依赖的第三方许可证声明。
- 移除 OpenCC4J 及其传递依赖，使用宿主已有 JNA 调用 Windows 原生繁简转换，缩减发行包体积。

## 0.9.0

- “频谱图”按钮在进入和退出页面时保持固定文字，并与“匹配选中”“保存当前”“保存选中”统一宽度。
- 浅色模式调整为 243/249 两级背景；右键菜单、二级菜单和下拉菜单统一使用圆角弹层及圆角选中条。
- 页面动画按工作台所在显示器的当前刷新率调度；音频读取、文件夹扫描、标签匹配、候选搜索及频谱计算均在守护工作线程执行。
- 工作台启动后预布局歌曲列表展开表头、日志、歌词预览、封面预览和频谱页面框架，切换页面时不再重复创建常驻组件。
- 歌词预览退出时取消候选搜索及歌词下载并清空候选；封面预览退出时释放候选图片和下载缓存；频谱退出时取消解码、清空图像及软缓存，下次进入重新加载。
- 封面预览的当前封面框架改为常驻复用，歌曲列表展开表头改为单实例复用，减少反复切换产生的组件和监听器。
- 页面切换加入异常恢复，遇到布局或动画异常时安全返回主工作台并写入临时日志。
- 动画快照改用当前显示设备兼容图像并在结束后立即释放，平滑滚动计时器跟随显示刷新率且随窗口销毁注销。
- 音频手动导入改为动态多线程读取，避免大量文件标签解析阻塞界面线程。
- 工作台关闭时取消扫描、匹配、预览和频谱任务，停止所有动画与滚动计时器，清空图片、表格、文本和日志，并解除启动器对旧窗口的引用。
- 文件夹选择窗口关闭后清理滚动监听器、列表模型和组件树；插件停止时释放当前媒体引用及封面缓存。
- 工作台对 FlatLaf 的进程级设置增加引用计数和关闭恢复，频谱仅释放插件自行创建的 BASS 解码流，不初始化或关闭 SPW 的全局音频引擎。
- 移除 OpenCC4J 及其传递依赖，繁简转换改用 SPW 已提供的 JNA 调用 Windows 原生转换接口，显著缩小插件包。

## 0.8.9

- “匹配选中”左侧新增普通样式的“频谱图”按钮，支持从工作台进入独立频谱页面。
- 复用 SPW 已加载的 BASS 音频引擎进行解码和 FFT，不额外打包 FFmpeg、BASS 或 JNA。
- 立体声音频按左、右两栏分别绘制声道频谱；单声道自动使用完整区域。
- 左右声道下方增加横跨两栏的连续时间轴柱状总览，不再生成缩略频谱或波形线。
- 频率轴改到右侧并按 4 等分显示刻度，不在频谱内部绘制横向辅助线。
- 收紧频谱动态范围并压暗中低能量区域，配色调整为参考图风格的黑紫、蓝、青绿、黄橙渐变。
- 去掉频谱页内歌名和返回按钮；工具栏“频谱图”按钮在进入后切换为“返回”。
- 频率刻度移到左侧；主频谱按底部橘黄到顶部青紫着色，底部柱状总览使用相反方向的纵向渐变。
- 频谱页面、首帧图像和返回过程加入淡入淡出，计算与界面绘制异步执行。
- 频谱结果按文件状态和绘制尺寸进行轻量软缓存，关闭页面或工作台时取消任务并释放解码流。
- 审核临时日志系统，为音乐库扫描、音频导入、聚合匹配、封面与歌词预览、频谱生成、封面导入导出及标签写入补充开始、结果、数量、异常和耗时信息。
- 批量匹配结束后追加汇总行，明确请求、实际处理、完整、预览、失败和跳过数量。
- 封面预览按来源记录返回结果；所有来源均无可用图片时输出明确的红色汇总错误。
- 日志仅保存在工作台内存中；关闭窗口时中止相关异步任务、清空日志文档，不创建日志文件。

## 0.8.8

- 识别 `-《影视作品》主题曲` 等媒体说明后缀，结合艺术家和时长校验核心标题。
- 括号、现场版、伴奏版、钢琴版、环绕版或混音版等文字统一作为标题附加信息；核心歌名相同时提高相关度，不再据此拒绝匹配。
- 规范化不间断空格，减少内容相同却显示“相似匹配”的日志。
- 将日志中的“候选”改为“平台搜索到的歌曲/结果”，明确说明是否更新预览。
- 封面预览中的“封面候选”统一改为“封面匹配结果”。

## 0.8.7

- 修复中文短艺术家名称完全一致仍被拒绝的问题。
- 聚合源按标题、艺术家、版本标记和时长逐来源校验，并输出具体拒绝原因。
- 现场版、伴奏版、纯音乐版和混音版不再与普通版互相覆盖。
- 聚合匹配、单源匹配及封面预览统一使用多级标题与时长校验。

## 0.8.6

- 文件夹管理窗口的路径列表、边框和内部背景改为完整圆角样式；路径标题精简为“已保存路径”。
- 深色模式背景层级调整为 RGB 32/32/32 与 39/39/39。
- 底栏左侧增加“添加”按钮，与顶部文件夹选择按钮作用相同；旁边保留“删除”。
- 日志切换移除高度动画计时器，提示行双击后立即展开或收回，避免动画状态造成偶发无响应。
- 修复日志高度已改变但顶层窗口未立即布局、必须最大化后才显示的问题；展开高度增加至 183 像素。
- 已保存路径列表支持按 Delete 键删除当前选中项。
- 日志展开/收回改为立即递归重排整个窗口组件树，并在当前鼠标事件结束后再次确认布局，不再依赖最大化触发顶层 BorderLayout。
- 日志展开和收回加入与歌曲列表一致的二次缓入缓出动画，每帧执行确定性布局。
- 日志正文改用 MiSans，提示行文字加粗；两侧方向提示改为 Java2D 对称双箭头，不再依赖全角字体字符。
- 整理轮换提示与状态文案，尽量以文字或数字结尾，避免末尾全角符号造成视觉偏移。
- 提示文字取消加粗并放大至 14px，左右 Java2D 箭头改为与文字同色。
- 音乐库扫描和批量匹配线程数按 CPU 逻辑线程与实际任务数量动态分配，启动日志显示真实可用线程数。
- 歌词窗格右上角新增“展开”，支持动画进入全页歌词视图；全页右上角显示“收回”。
- “展开”按钮改为浮在最内层歌词框右上角，默认低透明度，鼠标悬停后清晰显示。
- 全页歌词使用专用连续动画：其他区域整体向左推出，原歌词框快照持续平移并拉伸至全页位置，过程中不再消失后重现。
- 歌词展开/收回改为同一个浮动按钮实例，随歌词视图切换位置并更新文字。
- 所有按钮和输入框统一为 10px 中等圆角，移除原先接近胶囊形的半圆样式。
- 歌词展开/收回按钮移到工作区固定浮层，动画期间不移动、不缩放、不重新创建；默认透明度提高并向左避开滚动条。
- 歌词连续动画只拉伸视口内容的水平方向，保持 Y 坐标与高度；滚动条留在固定右侧，不参与截图缩放。
- 歌词动画进一步改为最内层完整窗格的水平九宫格切片：右侧 96px 的按钮、滚动条与边框固定，只有左侧内容区向左延展。
- 全页歌词目标页复用“标签信息”外框及相同的上下、右侧内边距；动画改为右边界固定的水平揭示，外框和内层歌词框只改变左边界。

## 0.8.5

- 日志提示分隔行重做为无子控件的完整绘制条，展开后位于主区域与日志之间，收回后位于窗口底部；整条区域双击均可切换。
- 文件夹管理改为自定义对话框，底部同一行左侧为“删除选中路径”，右侧为“确认”“取消”。
- 已保存路径改为可逐行点击、多选的列表，左下角按钮文字简化为“删除”。

## 0.8.4

- 日志底栏改为双击展开/收回，移除占用高度的独立按钮，并以淡色双箭头提示交互。
- 歌曲列表上方改为左右并列的“添加音频”和“清空列表”按钮。
- 匹配完成提示旁保留日志展开箭头，日志展开高度增大，并以默认色、黄色、红色区分成功、警告与失败。
- 歌词改为逐字优先、普通歌词兜底；普通歌词以黄色降级说明记录，完全无歌词仍判定失败并保留勾选。
- 日志仅为每行末尾的结果说明着色，成功内容保持默认文字色；展开高度调整为 180 像素。
- 标签完整性只要求标题、艺术家和专辑，其他扩展字段缺失不再产生警告。
- 封面候选不再把所有平台统一缩放为 800×800；网易云读取原图，其他平台按各自可用规格请求并显示实际解码尺寸。
- 四个平台的封面候选均优先请求源站原始尺寸，源站不支持时才回退兼容地址；候选固定按“有结果、无结果、加载中”排序。
- 列表与封面预览动画统一采用两端较快、中段缓行的节奏曲线。
- 来源顺序统一为 QQ、网易云、Apple、酷狗，并新增默认“聚合源”；聚合歌词先遍历四源逐字歌词，再遍历普通歌词。
- 全部动画改为标准二次缓入缓出曲线并缩短至约 210–220 毫秒，移除中途近乎停顿的节奏。
- 日志底栏加入 180 毫秒高度展开/收回动画。
- 聚合源改为按顺序增量补齐缺失内容，标题、艺术家、专辑、封面和逐字歌词齐全后立即停止后续请求。
- 日志中的“本地”改为更直观的“当前文件”。
- 过滤来源以 `0`、`0/0`、`unknown` 等表示的未知标签值，避免写入碟号、风格等字段。
- 日志改为每首歌曲处理完成后实时追加，并修复状态文字或箭头区域无法再次双击展开的问题。
- 标题对比说明改为“原标签标题”，不匹配时明确提示未写入。
- 聚合源在接纳每个平台结果前先校验标题，错误候选会被丢弃并继续查询后续来源，避免首个来源的无关歌曲阻断聚合匹配。
- 封面和歌词窗格的单项右键菜单移除聚合源，仅保留四个平台。
- 日志底栏改为独立计时识别双击，避免收起后的布局变化导致无法再次展开。
- 日志收回后保留零高度组件，不再切换为不可见状态，确保底栏双击热区和再次展开始终有效。
- 批量匹配改为最多 8 个受控并发任务，并阻止同一时间重复启动第二批匹配。
- 近似匹配日志改为“已接受相似标题”，只显示候选及省略附加文字的说明，不再重复原标题。
- 日志改用与歌词一致的圆角内容框和沉浸式滚动条；双击由窗口级事件监视器锁定提示分隔行，展开与收回时热区始终跟随该行。
- 工作台启动时记录版本、CPU 线程、并发数和标签源加载状态，音乐库扫描开始、完成或异常也会实时写入日志。
- 音乐文件夹管理窗口左下角新增“删除选中路径”按钮。
- 封面预览动画开始时原地隐藏列表和标签信息外框，避免标题边框被切片后随窗格飞出。

## 0.8.3

- 移除工具栏“全选/取消全选”，在普通与详细表头首列增加全选、取消全选及部分选中状态复选框。
- 封面预览将已返回结果集中排列在仍在加载的卡片之前。
- 底栏增加可展开日志，按歌曲、来源、标签记录无候选、标题不匹配、歌词/封面缺失、空字段、近似匹配及异常详情。

## 0.8.2

- 列表展开与收回改为快照动画，真实 JTable 仅在切换开始时布局一次，避免大量歌曲逐帧重排造成卡顿。
- 未展开列表表头也持续显示上下留空的淡色列分隔线。
- 动画结束或反向切换时立即释放旧快照。

## 0.8.1

- 修复列表收回时布局未逐帧刷新，并支持展开动画中立即反向收回。
- 普通与详细列表统一使用圆角表头；详细模式增加上下留空的淡色列分隔线。
- 封面移动与缩放动画始终使用圆角裁剪，避免过渡途中出现直角。

## 0.8.0

- 动画刷新率跟随当前显示器（最高 240 Hz），使用基于时间的平滑插值。
- 双击预览页当前封面可动画返回，封面信息不再显示格式字段。
- 歌曲列表底部“清空”改为“展开/收回”，展开时右侧区域平滑移出并让列表铺满工作区。
- 展开列表支持拖动列顺序和调整列宽，默认增加时长、位深、比特率。

## 0.7.2

- 候选网格强制跟随视口宽度，修复窗口状态下右侧内容出界。
- 原封面在整个切换动画中连续移动和缩放，不再通过两张封面交叉淡入衔接。
- “封面候选”标题移至底部并与收回按钮同行，增加封面纵向空间。
- 动画降至约 30 FPS、减少插值开销，并在结束后立即释放快照和移动图像引用。

## 0.7.1

- 聚焦动画改为歌曲列表向左、标签区向下、歌词区向右分别退场与返回。
- 当前封面移至左侧并按原比例显示，增加导入、导出、格式、像素和大小信息。
- 右侧候选区改为每个来源请求并展示至少 3 条结果，每张候选显示格式、像素和大小。
- 返回按钮固定在大框右下角，补充酷狗搜索封面字段回退。

## 0.7.0

- 双击封面进入同窗口聚焦预览模式，带滑动淡入过渡，并行展示各音乐源命中的封面候选。
- 支持一键应用候选封面并动画返回工作台。
- 预览关闭或窗口销毁时取消候选任务、停止动画计时器并释放临时封面与过渡图像。

## 0.6.2

- 歌曲列表右键菜单增加批量“匹配封面”和“匹配歌词”，与“匹配全部”一样仅作用于勾选歌曲。

## 0.6.1

- 标题标签为空时改用音频文件名搜索，并校验远端标题相关性，防止同一艺术家的歌曲被批量覆盖为同一首。
- 封面区域右键菜单增加导入封面与导出封面。

## 0.6.0

- 配置页合并重复的音乐文件夹项目，通过一个带资源管理器图标的目录管理器选择并保存多行路径。
- 目录选择器会优先展开已保存目录，工作台启动时支持合并扫描多个音乐文件夹。
- 标签与歌词区域合并为连续大框，移除两栏外框间隔并增加两侧内容留白。
- 合并框仅保留一个“标签信息”标题，并修复标签表单底部空白。
- 使用多路径目录管理选择器；右键匹配严格限定在歌曲列表、封面和歌词三个对应区域，并加入独立封面匹配。
- 修复未打开工作台时目录管理窗口只有标题栏的问题；批量匹配实时显示数量与百分比，并在完成后仅保留失败项勾选。

## 0.5.5

- QQ 音乐搜索由旧网页接口切换为与 LDDC 一致的 MusicU 搜索。
- QQ 搜索不再只取首条结果，改为在 20 个候选中综合匹配标题、版本名与艺术家，并自动跳过没有逐字歌词的版本，修复带 `(Movie ver.)` 等版本后缀的歌曲匹配失败。

## 0.5.4

- 按 LDDC 的逐字 LRC 规则去除行首、相邻字结束与开始处的重复时间戳，避免 SPW 误判为额外翻译行。
- 过滤 `//` 等占位译文、与原文相同的重复译文及作词/作曲/编曲制作信息行译文。

## 0.5.3

- 右键匹配固定只作用于当前显示歌曲；上方“匹配选中”仍只作用于勾选歌曲，二者互不影响。
- 为每首歌曲增加匹配请求令牌，后发请求优先，过期响应不再覆盖新歌词。
- 修复异步完成时刷新错误歌曲、匹配成功但歌词空状态未消失，以及重复匹配后歌词不更新的问题。

## 0.5.2

- 移除网易云、QQ、酷狗与 Apple Music 的全部罗马音请求、解析和写入。
- 原文统一输出方括号词级时间戳的逐字 LRC，译文仅使用整行时间戳。
- 不再输出增强 LRC，也不再在缺少真实逐字数据时回退普通逐行 LRC。

## 0.5.1

- Apple Music 源接入同步歌词服务，优先使用多角色增强 LRC、增强 LRC 或词级时间数据。
- Apple Music 逐字数据不可用时回退到普通 LRC。

## 0.5.0

- 对照 Lyrico 与 LDDC，接入网易云 YRC、QQ QRC、酷狗 KRC 的真实逐字歌词获取与解析流程。
- QQ 歌词改用 MusicU 获取加密 QRC、翻译与罗马音，并进行 3DES 与 zlib 解码。
- 酷狗歌词改用歌词搜索、KRC 下载、异或与 zlib 解码，解析内嵌翻译和罗马音轨道。
- 各源在逐字接口不可用时仍会回退到普通 LRC，避免完全匹配不到歌词。

## 未发布

- 根据 LDDC 的 GPL-3.0-only 要求，将项目主许可证由 MIT 修正为 GPL-3.0-only。
- 增加第三方项目及依赖许可证声明。

## 0.4.1

- 搜索改为输入时即时筛选，删除全部搜索文字后立即恢复完整列表。
- 分区域右键匹配改为只作用于当前歌曲。
- 修复歌词匹配成功后被当前空白编辑内容覆盖、歌词窗口仍为空的问题。

## 0.4.2

- 即时搜索后的按钮改为“清空”，用于清空搜索词并恢复完整列表。
- 切换歌曲时，所有标签字段及注释的光标统一回到文本开头。
- 参照 LDDC 的多轨歌词结构，网易云歌词优先保留 YRC 逐字时间，并按原文行时间合并翻译与罗马音。
- 修复网易云逐字歌词命中后直接返回、导致翻译和罗马音被丢弃的问题。

## 0.4.0

- 音乐文件夹扫描改为受控多线程并行读取，线程数会根据处理器数量自动调整，最高使用 8 个工作线程。
- 扫描结果统一回到界面线程更新，避免并发修改列表导致界面异常。

## 0.3.1

- 歌词为空时，在歌词区域居中提示可通过右键单独匹配歌词源。
- 修复切换歌曲后歌词区域默认停留在底部的问题。

## 0.3.0

- 新增歌曲标签模糊搜索、右键清空列表和单元格完整内容提示。
- 新增开源项目入口和插件主页信息。
- 优化标题栏、匹配结果状态色、主题配色及列表布局。
- 修复选择歌曲后标签区域未回到顶部等问题。
